# 郑敬耀 — 第4周 周六～周日｜安全基础 + 网站内容大整合

> 目标：加固云服务器安全，全面检查网站所有页面，确保每个板块都有实质内容
> 预计时间：2天（每天3-4小时）
> 产出：安全笔记 + 服务器安全加固 + 网站全面完善

---

## 周六（约3.5小时）｜ 安全基础学习 + 服务器加固

### 步骤1：理解基础安全概念（30分钟阅读）

搞网络安全不需要"精通"，但作为运维岗的面试准备，以下是你应该能说出原理的东西：

```
三层防护体系（从外到内）：

第一层 → UFW 防火墙：控制"谁可以连到我"（IP和端口级别）
第二层 → fail2ban：    控制"暴力破解"（多次失败自动封IP）
第三层 → SSH 安全配置： 控制"谁可以登录"（密钥认证、禁root登录）
```

**UFW（Uncomplicated Firewall）**：
- Linux 上的防火墙管理工具
- 默认拒绝所有入站连接，只开放你指定的端口
- `ufw allow 22` = 允许 SSH
- `ufw deny 3306` = 拒绝 MySQL
- `ufw enable` = 开启防火墙

**fail2ban**：
- 监控日志文件，发现多次失败尝试就自动封禁来源 IP
- 默认监控 SSH，有人暴力破解你的密码，3-5次失败就封 IP 10分钟
- 可以自定义监控 Nginx、MySQL 等服务

**SSH 密钥登录**：
- 比密码安全得多——密码可能被暴力破解，密钥不可能
- 公钥放在服务器，私钥在自己电脑上
- 配置完成后可以完全禁用密码登录

---

### 步骤2：SSH 密钥登录配置（40分钟）

> 你已经做了服务器 ↔ GitHub 的 SSH 配置，现在是本地 PC → 服务器的 SSH 配置。

#### 2.1 本地生成密钥对（如果之前没做过）

```bash
# 本地 PC，Git Bash
ssh-keygen -t ed25519 -C "z71271-pc"
# 一路回车

# 查看公钥
cat ~/.ssh/id_ed25519.pub
```

#### 2.2 把公钥传到服务器

```bash
# 方法1：用 ssh-copy-id（最简单）
ssh-copy-id root@8.138.195.245
# 输入服务器密码，自动把公钥复制过去

# 方法2：手动复制（如果 ssh-copy-id 不可用）
cat ~/.ssh/id_ed25519.pub | ssh root@8.138.195.245 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

#### 2.3 测试密钥登录

```bash
# 应该不需要输入密码
ssh root@8.138.195.245
# 直接登录 → SSH 密钥配置成功 ✅
```

#### 2.4 加固 SSH 配置（在服务器上操作）

```bash
ssh root@8.138.195.245

# 备份原配置
cp /etc/ssh/sshd_config /etc/ssh/sshd_config.bak

# 编辑 SSH 配置
vim /etc/ssh/sshd_config
```

找到并修改以下参数：

```ini
# 1. 禁止 root 通过密码登录（但保留密钥登录）—— 重要！
PermitRootLogin prohibit-password

# 2. 禁止密码登录（只允许密钥登录）—— 等确认密钥能用后再改！
# PasswordAuthentication no

# 3. 修改 SSH 端口（可选，减少自动化扫描）
Port 22
# 改成 Port 2222 可以大幅减少自动攻击

# 4. 禁止空密码
PermitEmptyPasswords no

# 5. 最大登录尝试次数
MaxAuthTries 3
```

**⚠️ 重要提醒**：先不要改 `PasswordAuthentication no`——等你完全确认密钥登录能用，并且做好备份计划后再改。万一密钥丢了还能用密码救回来。

```bash
# 检查配置语法
sshd -t
# 无输出 = 配置正确

# 重载 SSH 服务
systemctl reload sshd
```

**测试**：另开一个终端，用密钥登录确认还能连上，再关掉当前终端。

---

### 步骤3：配置 fail2ban（30分钟）

```bash
ssh root@8.138.195.245

# 确认已安装
apt install -y fail2ban

# 查看默认配置
cat /etc/fail2ban/jail.conf | head -50
```

#### 3.1 创建自定义配置

不要直接改 `jail.conf`（升级会覆盖），创建 `jail.local`：

```bash
cat > /etc/fail2ban/jail.local << 'EOF'
[DEFAULT]
# 封禁时间（秒）：设为 3600 = 1小时
bantime = 3600

# 查找时间窗口（秒）：在这个时间窗口内失败 findtime 次就封
findtime = 600

# 最大失败次数
maxretry = 5

# 忽略的 IP（你自己的 IP，不会被封）
ignoreip = 127.0.0.1/8

[sshd]
enabled = true
port = ssh
logpath = %(sshd_log)s
maxretry = 3

# 如果你的网站有登录功能，可以加：
[nginx-login]
enabled = false
port = http,https
filter = nginx-login
logpath = /var/log/nginx/access.log
maxretry = 5
EOF
```

#### 3.2 启动并验证

```bash
# 重载配置
systemctl restart fail2ban

# 查看状态
fail2ban-client status
# 应该显示: |- Number of jail: 1
#           `- Jail list: sshd

# 查看 SSH jail 详情
fail2ban-client status sshd
# 显示:
#   |- Currently failed: 0
#   |- Currently banned: 0
#   |- Total banned: 0

# 模拟测试（故意输错密码3次，看会不会被封）
# 另开终端：ssh root@8.138.195.245，故意输错密码3次
# 再查看 status，应该有 banned IP
```

解封命令（万一自己被封了，用其他方式登录服务器）：
```bash
fail2ban-client set sshd unbanip 你的IP
```

---

### 步骤4：UFW 防火墙配置（20分钟）

```bash
# 查看当前状态
ufw status verbose

# 如果没开启，按顺序执行：
ufw default deny incoming    # 默认拒绝所有入站
ufw default allow outgoing   # 默认允许所有出站

# 开放必要端口
ufw allow 22/tcp             # SSH
ufw allow 80/tcp             # HTTP
ufw allow 443/tcp            # HTTPS（如果将来用）
ufw allow 9999/tcp           # Docker 网站端口（如果还在用）

# 如果改了 SSH 端口，开放新端口
# ufw allow 2222/tcp

# 开启防火墙
ufw --force enable

# 验证
ufw status numbered
```

输出应该类似：
```
Status: active

     To          Action      From
     --          ------      ----
[ 1] 22/tcp      ALLOW IN    Anywhere
[ 2] 80/tcp      ALLOW IN    Anywhere
[ 3] 443/tcp     ALLOW IN    Anywhere
[ 4] 9999/tcp    ALLOW IN    Anywhere
```

---

### 步骤5：写安全笔记（30分钟）

```bash
cd ~/Desktop/网站代码/z71.z71.pw/notes
```

```markdown
cat > server-security.md << 'EOF'
# 云服务器安全加固笔记

> 记录时间：2026年8月 | 状态：已完成

## 安全加固做了什么

| 安全措施 | 工具 | 作用 |
|----------|------|------|
| 防火墙 | UFW | 默认拒绝所有入站，只开放必要端口（22/80/443） |
| 防暴力破解 | fail2ban | 3次 SSH 登录失败自动封 IP 1小时 |
| SSH 密钥登录 | OpenSSH | 禁用密码登录，仅允许密钥认证 |
| 最小权限 | Linux 权限 | 非必要不用 root 跑服务 |

## 防火墙（UFW）

```bash
# 查看状态
ufw status verbose

# 默认策略
ufw default deny incoming   # 拒绝所有入站
ufw default allow outgoing  # 允许所有出站

# 按需开放端口
ufw allow 22/tcp   # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp  # HTTPS

# 删除规则
ufw delete 3       # 删除第3条规则

# 临时关闭
ufw disable
```

## 防暴力破解（fail2ban）

```bash
# 查看状态
fail2ban-client status
fail2ban-client status sshd

# 解封某个 IP
fail2ban-client set sshd unbanip 1.2.3.4

# 日志位置
tail -f /var/log/fail2ban.log
```

### 配置要点

`/etc/fail2ban/jail.local`:
- `bantime = 3600` — 封禁1小时
- `findtime = 600` — 10分钟内的失败累计
- `maxretry = 3` — 失败3次就封

## SSH 安全

### 推荐配置（/etc/ssh/sshd_config）

```ini
# 禁止 root 密码登录（保留密钥登录）
PermitRootLogin prohibit-password

# 完全禁止密码登录（确认密钥可用后再开）
PasswordAuthentication no

# 禁止空密码
PermitEmptyPasswords no

# 限制尝试次数
MaxAuthTries 3
```

### 密钥登录配置

```bash
# 本地生成密钥
ssh-keygen -t ed25519

# 复制公钥到服务器
ssh-copy-id root@8.138.195.245

# 之后直接 ssh root@8.138.195.245 无需密码
```

## 为什么禁用密码登录

密码可能被暴力破解（试 10000 次总有一次对的）。SSH 密钥长度 256 位，暴力破解需要的时间比宇宙年龄还长。这就是为什么生产环境都要求密钥登录。

## fail2ban vs UFW 的区别

- **UFW**：静态防火墙，"端口 22 可以连，端口 3306 不能连"
- **fail2ban**：动态防火墙，"有人连续输错3次密码，自动封他 IP 1小时"

两者配合使用：UFW 做第一层过滤，fail2ban 做第二层动态拦截。

## 踩坑记录

### 坑1：改了 sshd_config 后连不上服务器
原因：配置有语法错误。预防措施：改之前备份，改完后 `sshd -t` 检查语法，另开终端测试登录。

### 坑2：fail2ban 配置不生效
原因：改的 `jail.conf` 被升级覆盖了。应该改 `jail.local`（优先级更高，不会被覆盖）。

### 坑3：ufw enable 后 SSH 断了
原因：没有先 `ufw allow 22`。教训：开启防火墙前一定先确认 SSH 端口在白名单里。
EOF
```

---

## 周日（约3.5小时）｜ 网站内容大整合

---

### 步骤6：逐页检查网站（1.5小时）

> 打开浏览器，逐一访问你的网站每个页面，拿着下面的清单逐项检查。

```bash
# 本地预览
cd ~/Desktop/网站代码/z71.z71.pw
python -m http.server 8000
```

#### 6.1 首页（index.html）

访问 `http://localhost:8000/`

检查项：
- [ ] typewriter 动画包含 AI 关键词
- [ ] 导航栏链接正确（6个入口：首页/技术简历/AI能力/销售简历/笔记/联系）
- [ ] GitHub 链接正常跳转
- [ ] 整体排版在手机端（F12 → 手机模式）是否正常

需要改进的地方（记下来，等下改）：
1. ...
2. ...

#### 6.2 技术简历（tech.html）

检查项：
- [ ] 导航栏有 AI 入口
- [ ] 技术栈包含了 AI、Docker、Python
- [ ] 项目经历包含 `enterprise-network-lab`
- [ ] 数据文件 `assets/data/tech.json` 内容和页面显示一致

需要更新：打开 `assets/data/tech.json`，确认技能和项目是最新的。

#### 6.3 AI 能力页（ai.html）

检查项：
- [ ] 5个板块都正常渲染（工具链/部署方案/RAG/技能/项目）
- [ ] 项目状态正确（已完成的标记为"已完成"）
- [ ] RAG 描述准确
- [ ] 技能列表包含 Docker、Ollama、RAG、ComfyUI
- [ ] 架构层级图正确展示

#### 6.4 销售简历（sales.html）

检查项：
- [ ] 导航栏通路正确
- [ ] 内容是否还需要更新
- [ ] 可以考虑加上"AI技术销售"的能力描述

#### 6.5 笔记页（notes.html）

访问 `http://localhost:8000/notes.html`

检查项：
- [ ] 4篇笔记都显示在列表里（dockerfile-guide / ollama-deploy / rag-deploy / ai-toolchain）
- [ ] 点击每篇笔记能正确渲染 Markdown
- [ ] 代码块有高亮背景
- [ ] 表格正确显示
- [ ] "返回笔记列表"链接有效

可能需要新增的笔记：
- [ ] `comfyui-deploy.md`（第3周写的）
- [ ] `server-security.md`（刚才写的）
- [ ] 检查 notes.html 里的 `notesMeta` 数组是否包含了所有笔记

#### 6.6 Linux 速查表（linux-cheatsheet.html）

访问 `http://localhost:8000/linux-cheatsheet.html`

检查项：
- [ ] 7个分类都正常显示
- [ ] code 标签有黄色高亮
- [ ] 表格电脑/手机端都正常

#### 6.7 联系页（contact.html）

检查项：
- [ ] 联系信息准确
- [ ] GitHub 链接正确（https://github.com/z71271）
- [ ] 网站链接正确（http://8.138.195.245）
- [ ] 如果 `messages.json` 有留言功能，确认后端还在工作

---

### 步骤7：修复发现的问题（1.5小时）

根据上一步的检查结果，逐个修复。

#### 7.1 更新 notes.html 的笔记列表

确保所有笔记都在 `notesMeta` 数组中：

```javascript
const notesMeta = [
    {
        file: 'dockerfile-guide.md',
        title: 'Dockerfile 学习笔记',
        date: '2026-07',
        tags: ['Docker', '运维'],
        desc: '从 docker run 到 docker build — 学会写 Dockerfile 的 7 个核心指令'
    },
    {
        file: 'ollama-deploy.md',
        title: 'Ollama 本地大模型部署笔记',
        date: '2026-07',
        tags: ['AI', 'Ollama'],
        desc: '在 RTX 3080 上部署 qwen2.5、llama3.1、deepseek-r1 三模型的完整记录'
    },
    {
        file: 'rag-deploy.md',
        title: 'RAG 知识库部署笔记',
        date: '2026-07',
        tags: ['AI', 'RAG'],
        desc: '用 AnythingLLM 搭建本地 RAG 知识库，让 AI 基于私有文档回答问题'
    },
    {
        file: 'ai-toolchain.md',
        title: '我的 AI 工具链总结',
        date: '2026-07',
        tags: ['AI', '架构'],
        desc: 'Ollama + Open WebUI + AnythingLLM + Python 脚本 — 完整的本地 AI 工具链梳理'
    },
    {
        file: 'comfyui-deploy.md',
        title: 'ComfyUI / Stable Diffusion 部署笔记',
        date: '2026-08',
        tags: ['AI', '多模态'],
        desc: '在 RTX 3080 上部署 ComfyUI，实现文生图和图生图工作流'
    },
    {
        file: 'server-security.md',
        title: '云服务器安全加固笔记',
        date: '2026-08',
        tags: ['Linux', '安全'],
        desc: 'UFW 防火墙 + fail2ban + SSH 密钥登录，三层防护体系'
    }
];
```

#### 7.2 更新 tech.json

确认 `assets/data/tech.json` 包含了最新的技能和项目：

```json
{
    "skills": [
        {"name": "网络工程", "level": "90%", "items": ["eNSP", "OSPF", "BGP", "VLAN", "ACL", "NAT"]},
        {"name": "Linux运维", "level": "80%", "items": ["CentOS/Ubuntu", "Nginx", "MySQL", "systemd", "Shell脚本"]},
        {"name": "Web前端", "level": "75%", "items": ["HTML5", "CSS3", "JavaScript", "响应式设计"]},
        {"name": "Docker", "level": "75%", "items": ["Dockerfile", "docker-compose", "镜像构建"]},
        {"name": "AI本地部署", "level": "70%", "items": ["Ollama", "Open WebUI", "RAG", "ComfyUI"]},
        {"name": "Python", "level": "60%", "items": ["FastAPI", "OpenAI SDK", "爬虫", "SSH自动化"]}
    ]
}
```

#### 7.3 统一所有页面的导航栏

最终导航栏应该是（7个入口）：

```
<ZJY/> | 首页 | 技术简历 | AI能力 | 销售简历 | 笔记 | 速查 | 联系
```

逐个检查以下文件中的 `<ul class="nav-links">`：
- `index.html`
- `tech.html`
- `ai.html`
- `sales.html`
- `notes.html`
- `linux-cheatsheet.html`
- `contact.html`

每个文件的导航栏都应该包含相同的链接列表。

#### 7.4 修复死链

用浏览器打开每个页面，逐个点击导航栏链接，确认没有 404。

---

### 步骤8：最终提交 + 服务器部署（30分钟）

#### 8.1 本地提交

```bash
cd ~/Desktop/网站代码/z71.z71.pw

# 看看改了哪些文件
git status

# 添加所有变更
git add .

# 提交
git commit -m "第4周收尾：安全笔记+网站全面检查+导航栏统一+内容完善"

# 推送
git push origin main
```

#### 8.2 服务器同步

```bash
ssh root@8.138.195.245
cd /www/wwwroot/z71.z71.pw
git pull origin main
```

#### 8.3 服务器端最终验证

在浏览器逐一访问（用你的实际端口）：

- `http://8.138.195.245/` — 首页 ✅
- `http://8.138.195.245/tech.html` — 技术简历 ✅
- `http://8.138.195.245/ai.html` — AI能力 ✅
- `http://8.138.195.245/sales.html` — 销售简历 ✅
- `http://8.138.195.245/notes.html` — 笔记列表 ✅
- `http://8.138.195.245/notes.html?post=dockerfile-guide.md` — 单篇笔记渲染 ✅
- `http://8.138.195.245/linux-cheatsheet.html` — Linux速查表 ✅
- `http://8.138.195.245/contact.html` — 联系页 ✅

---

### 步骤9：第4周总结（15分钟）

#### 9.1 GitHub 仓库清单（截至第4周）

| # | 仓库 | 内容 | 第几周 |
|---|------|------|--------|
| 1 | `z71271/z71271` | 个人网站源码 | 第1周 |
| 2 | `z71271/ai-gateway` | FastAPI AI 网关 + systemd 服务 | 第3周 |
| 3 | `z71271/net-auto-config` | 网络设备批量配置脚本 | 第3周 |
| 4 | `z71271/simple-crawler` | Python 爬虫集合 | 第3周 |
| 5 | `z71271/enterprise-network-lab` | 企业网络拓扑设计 | 第4周 |
| 6 | `z71271/shell-scripts` | Shell 脚本合集 | 第4周 |

**6个 GitHub 仓库**，覆盖 4 个技术方向。

#### 9.2 网站内容清单

| 页面 | 内容 | 状态 |
|------|------|------|
| 首页 | 个人介绍 + 双简历入口 + typewriter | ✅ |
| 技术简历 | 网络/Linux/Docker/AI/Python + 项目经历 | ✅ |
| AI能力 | 工具链/部署/RAG/技能/项目 + 架构图 | ✅ |
| 销售简历 | 销售经验 + AI技术销售方向 | 🔲 待更新 |
| 笔记 | 6篇技术笔记（Docker/Ollama/RAG/工具链/ComfyUI/安全） | ✅ |
| Linux速查 | 7大类命令 | ✅ |
| 联系 | 联系信息 + GitHub | ✅ |

#### 9.3 技能成长对比

```
第1周开始前：
  Git:     不会  →  会 clone/add/commit/push/pull/branch/merge + SSH免密
  Docker:  只会run  →  能写Dockerfile + docker-compose + 7个核心指令
  Python:  仅小游戏  →  FastAPI后端 + OpenAI调用 + 爬虫 + SSH自动化
  AI:      玩过Ollama  →  完整工具链(Ollama/OpenWebUI/RAG/ComfyUI/SD)
  网站:    4个静态页  →  8个页面，6篇笔记，数据驱动渲染
  网络:    在简历里写"会OSPF"  →  完整的企业网络设计文档+配置+验证截图
  GitHub:  没账号  →  6个仓库，4方向，全部公开可访问

第4周结束时：
  不是"学过什么" → 是"做过什么、能展示什么"
```

---

## 周六～周日 检查清单

- [ ] SSH 密钥登录配置完成（不用密码就能连服务器）
- [ ] fail2ban 运行正常，SSH jail 有封禁统计
- [ ] UFW 防火墙开启，只开放必要端口
- [ ] 安全笔记 `server-security.md` 完成
- [ ] 网站7个页面全部检查通过
- [ ] 导航栏7个入口在所有页面中一致
- [ ] notes.html 包含了全部6篇笔记
- [ ] tech.json 技能列表是最新的
- [ ] 所有修改推送到 GitHub
- [ ] 服务器上网站可正常访问

---

## 第4周 完整产出物总结

| 产出 | 仓库/位置 | 类型 |
|------|-----------|------|
| eNSP 企业网络拓扑 | `enterprise-network-lab` | GitHub 仓库 |
| 网络设计说明 | `network-design.md` | 设计文档 |
| 完整配置命令 | `configs.md` | 配置文档 |
| systemd 服务文件 | `ai-gateway/ai-gateway.service` | 服务配置 |
| 一键部署脚本 | `shell-scripts/server-setup.sh` | Shell 脚本 |
| 日志分析脚本 | `shell-scripts/nginx-log-analyzer.sh` | Shell 脚本 |
| SSL 续期脚本 | `shell-scripts/ssl-renewal.sh` | Shell 脚本 |
| 服务器监控 | 服务器 `/root/monitor/` | 监控方案 |
| Linux 命令速查表 | 网站 `/linux-cheatsheet.html` | 网页 |
| 安全笔记 | 网站 `/notes/server-security.md` | 技术笔记 |
| 网站全面整合 | 8页面 + 6笔记 + 统一导航 | 网站升级 |

---

## 下一步（第5周预览）

```
周一～周三：网站全面升级（首页重构、项目展示页、响应式优化）
周四～周五：简历更新 + 面试话术准备 + GitHub Profile
周六～周日：开始投递实习！
```

> 4周时间，从"不会Git、不会Docker、Python只会小游戏"到"6个GitHub仓库、7页面网站、完整AI工具链、企业网络设计文档"——你已经不是4周前的你了。最后一周，把网站打磨到可以骄傲地发链接给面试官的程度。
